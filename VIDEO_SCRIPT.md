# STM32 多功能智能终端项目演示读稿

## 录制建议

建议视频时长控制在 4 到 6 分钟。演示时让摄像头同时拍到开发板和 OLED，切换功能时停留 2 到 3 秒，方便观众看清界面和按键反馈。

## 开场

大家好，这期视频展示的是我基于 STM32F103C8 开发的一套多功能智能终端系统。

这个项目以 OLED 为主要显示设备，通过按键完成菜单交互，集成了实时时钟、秒表、LED 调光、MPU6050 姿态检测、电池电量显示，以及小恐龙和贪吃蛇两个小游戏。

项目源码和 Keil 工程文件已经开源到 GitHub，链接会放在视频简介和评论区。

## 首页和菜单

现在可以看到系统首页，顶部显示日期和时间，底部是菜单和设置入口。

我使用 Key1 和 Key2 在选项之间移动，Key3 负责确认进入。整个菜单采用 OLED 图形界面实现，所有功能都通过同一套按键逻辑进行交互。

## 秒表和 LED

首先演示秒表功能。进入秒表后，可以使用按键选择开始、停止和清零，计时过程由定时器中断维护。

接下来是 LED 调光功能。通过 PWM 改变 LED 的占空比，长按 Key1 和 Key2 可以降低或提高亮度。这里可以看到亮度变化是连续的，而不是简单的开关控制。

## MPU6050 姿态检测

下面演示 MPU6050 姿态检测。第一次进入页面时，程序会进行静止校准，所以需要让设备保持水平和静止一小段时间。

校准完成后，页面显示 Roll、Pitch 和 Yaw。这里本项目主要对 Roll 和 Pitch 进行了零偏校准、单位换算和互补滤波，设备倾斜时对应角度会发生变化。

同时，水平仪页面会将姿态变化转换成 OLED 上的圆形指示效果，可以直观看到设备是否保持水平。

## 小恐龙游戏

接下来进入游戏菜单，先演示小恐龙游戏。

小恐龙会自动向前移动，Key1 用于跳跃。程序通过定时中断更新地面、障碍物、角色跳跃状态和分数，并使用碰撞检测判断游戏是否结束。

## 贪吃蛇游戏

下面演示贪吃蛇。蛇会自动移动，Key1 左转，Key2 右转，不能直接掉头。

蛇头和食物都是 4×4 像素，蛇身使用更小的 2×2 像素显示，方便区分蛇头和蛇身。游戏区域四周有明确边框，碰到边框或者撞到自己时游戏结束。

吃到食物后蛇身增长，分数增加，食物会重新生成在游戏区域内部。

## 历史最高分

游戏结束后，如果本局分数超过历史记录，程序会将新的最高分保存到 STM32 内部 Flash。

这里可以看到游戏菜单右上角的历史高分入口，进入后能够分别查看小恐龙和贪吃蛇的最高分。

程序只在产生新纪录时写入 Flash，并保存魔数和校验值，减少不必要的擦写。数据掉电后仍然保留。

## 动态动画

最后演示动态动画页面。第一个是原有的表情动画，可以看到闭眼、睁眼和停顿的完整节奏。

按 Key1 或 Key2 可以切换到第二个“我很臭吗”猫咪动画。这个动画由 30 张 64×64 的 OLED 位图组成，程序按照帧序号循环播放。

再次按 Key1 或 Key2 可以在两个动画之间切换，Key3 返回上一级菜单。

## 结尾

这个项目主要练习了 STM32 GPIO、定时器中断、I2C、ADC、PWM、RTC、OLED 图形显示、状态机、碰撞检测和内部 Flash 非易失存储。

后续如果继续完善，我会增加更系统的异常处理、参数配置和硬件测试记录。

感谢观看，项目源码和工程文件在 GitHub 开源，欢迎交流。
